import { create } from 'zustand';

export interface Detail {
  id: number;
  name: string;
  w: number;
  h: number;
  color: string;
  k1: string;
  k2: string;
  k3: string;
  k4: string;
  rotate: boolean;
}
